export function TextField({ id, label, helpText, error, type = 'text', autocomplete }) {const helpId=helpText?`${id}-help`:undefined; const errorId=error?`${id}-error`:undefined; const describedBy=[helpId,errorId].filter(Boolean).join(' ')||undefined; return (<div><label htmlFor={id}>{label}</label>{helpText&&<p id={helpId}>{helpText}</p>}<input id={id} name={id} type={type} autoComplete={autocomplete} aria-invalid={error?'true':undefined} aria-describedby={describedBy}/>{error&&<p id={errorId}>{error}</p>}</div>)}
export function Disclosure({ id, title, open, onToggle, children }) {return (<div><button type="button" aria-expanded={open} aria-controls={`${id}-panel`} onClick={onToggle}>{title}</button><div id={`${id}-panel`} hidden={!open}>{children}</div></div>)}
